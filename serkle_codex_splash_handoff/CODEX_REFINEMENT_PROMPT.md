# CODEX REFINEMENT PROMPT — Serkle opening polish

Review the implemented opening scene against:
- `MOTION_SPEC.json`
- `references/reference_animation_guide.png`

Now refine it without changing the overall architecture.

Priorities, in this order:
1. Remove any flash between native launch screen and animated splash.
2. Make the Serkle mark reconnect pixel-perfectly.
3. Reduce motion if it feels bouncy, playful, or busy.
4. Ensure the + is a temporary idea/connection moment, not the dominant mark.
5. Keep the background quiet and premium.
6. Make the exit into Home feel like one continuous scene, not a hard cut.
7. Verify 60fps and reduced-motion behavior.

Do not:
- rotate the logo
- use elastic/bouncy springs
- loop the animation
- add particles/confetti
- add extra text unless the current product already requires it
- keep users waiting for animation after the app is ready beyond the defined timing
- redesign the official Serkle symbol
